# --- Build / dépendances / plateformes ---
node_modules
.next
.vercel
.supabase
out
build
coverage
playwright-report
test-results
pnpm-lock.yaml

# --- Assets non formattables ---
*.min.js
*.min.css
gates/**/*.pdf
design/maquettes/*.html
design/copy/*.html

# --- Markdown auteur humain (Phase 1 baseline + docs métier) ---
# Prettier sur markdown peut mangler les tableaux ASCII et les sauts de ligne
# volontaires. On formate uniquement le code (TS, TSX, CSS, JSON sauf tokens).
*.md
README.md

# --- Design tokens (structure DTCG sensible) ---
design/tokens.json

# Artefacts générés par drizzle-kit (snapshots, journal, SQL de migration)
# Ne JAMAIS reformater manuellement — réécrits à chaque `drizzle-kit generate`.
src/db/migrations/

# SQL natif (extensions, RLS pgTAP) — pas de parser Prettier pour .sql,
# convention manuelle (uppercase keywords, indent 2 espaces).
*.sql

# Fixtures BOAMP committees -- produites par `pnpm db:fetch-boamp`,
# JSON.stringify(2) natif. Pas de reformatage prettier sur ces ~10 MB.
src/db/seed/fixtures/*.json
src/db/seed/distribution-report.json
src/db/seed/prod-seed-report.json

