# CC 2026-05-25 14h30 — Migrations 0007 + 0008 appliquées en prod

## Contexte
Les PRs #45 (Lot B architects) et #47 (Lots C+D templates) étaient mergées sur main.
Le code Vercel était déployé mais les migrations BDD n'étaient pas encore appliquées.

## Problème rencontré
`drizzle-kit migrate` échoue sur la machine Windows ARM64 de Steve :
- `allowBuilds: esbuild: false` (décision CTO 2026-05-18) + lockfile pin `@esbuild/win32-x64`
- Machine Steve = ARM64 → binaire incompatible

## Solution retenue
Application manuelle via Supabase SQL Editor + INSERT dans `drizzle.__drizzle_migrations`.

## Migrations appliquées
| Migration | Tag | Hash SHA256 | Contenu |
|-----------|-----|-------------|---------|
| 0007 | `0007_abnormal_ares` | `7e1dce0a...` | enum audit_action (3 valeurs) + colonnes rgpd_opposition sur architects |
| 0008 | `0008_chief_the_order` | `9eafeaf5...` | CREATE TABLE message_templates + organization_profiles |

## Bugs corrigés
- ✅ "Annuaire indisponible" sur /sourcing/architectes
- ✅ "Présentation société ne se sauvegarde pas" sur /sourcing/admin/societe

## Workflow prod établi
Future migrations : générer via `drizzle-kit generate` (CI/Linux), appliquer manuellement via SQL Editor + INSERT journal.
