# Note de suivi — Gate 6 étape 1

**Date** : 2026-05-11 04:13
**Auteur** : [DEV Alex] (Claude Code, sub-agent dev)
**Étape** : Gate 6 — étape 1 / 7 (`feat/init-nextjs`)
**Statut** : terminée localement, en attente de validation Board pour push.

## Objectif de l'étape

Bootstrap du repo `edifio-sourcing` greenfield avec la stack Gate 5 figée :
Next.js 14 App Router + TypeScript strict + Tailwind 3 + ESLint, plus l'outillage
Conventional Commits (Prettier + commitlint + husky) et les polices self-host
(Inter / Space Grotesk / JetBrains Mono via fontsource).

## Ce qui a été fait

1. **Préambule git par Yann (ps_operator)** : `git status`, `git log --oneline -5`. Repo
   greenfield, branche `feat/sourcing-mvp` héritée (laissée dormir), branche `main`
   active.

2. **pnpm activé via Corepack — shim utilisateur** : Corepack standard échoue (EPERM
   sur `C:\Program Files\nodejs`). Contournement validé : `corepack enable
   --install-directory $env:LOCALAPPDATA\pnpm-shims pnpm`. pnpm 11.0.9 actif sans
   installation système.

3. **Baseline Phase 1 commit sur main** : commit `chore: import Phase 1 baseline`
   regroupant CLAUDE.md, DECISIONS.md, gates/, specs/, design/, notes-de-suivi/,
   `.claude/agents/` et `.gitignore` initial. Sur main, pas encore poussé (deny
   `git push origin main` dans l'allowlist — `main` ne bouge que via PR).

4. **Branche `feat/init-nextjs`** créée depuis main mis à jour. Conventional Commits
   obligatoires.

5. **Bootstrap Next.js 14.2.35** : scaffold dans `scaffold-tmp/` puis remontée des
   fichiers config à la racine (CNA refuse les dossiers non vides). Versions épinglées
   dans `package.json` : Next 14.2.35, React 18.3.1, TS 5.9.3, Tailwind 3.4.19,
   ESLint 8.57.1. `packageManager: pnpm@11.0.9` figé. `engines.node >= 20`,
   `engines.pnpm >= 11`.

6. **Configuration pnpm 11** : `pnpm-workspace.yaml` avec `allowBuilds: unrs-resolver:
   true` pour autoriser le postinstall (`napi-postinstall`). Sans ça, l'install bloque
   par défaut (sécurité pnpm 11 contre les scripts inconnus).

7. **Outillage Conventional Commits** : Prettier 3.8 (avec `prettier-plugin-tailwindcss`),
   commitlint 21 (config-conventional), husky 9.1. Hook `commit-msg` valide chaque message
   de commit ; hook `pre-commit` lance `prettier --check` + `next lint` (rapide, non
   bloquant en CI mais bloquant côté dev).

8. **Polices self-host** : `@fontsource/inter` (400, 500, 600, 700), `@fontsource/space-grotesk`
   (500, 700), `@fontsource/jetbrains-mono` (400). Importées dans `src/app/layout.tsx`.
   Tailwind config mappe `font-sans` = Inter, `font-display` = Space Grotesk, `font-mono`
   = JetBrains Mono. Aucun appel à `fonts.googleapis.com`.

9. **`.env.example`** posé avec toutes les variables Gate 6 + futures (Anthropic, Brevo,
   Resend, Fly.io scraper, Postgres URL). Variables `[REQUIS G6]` clairement marquées.
   `.env.local` jamais committé (gitignore).

10. **`.gitignore` mergé** (scaffold CNA + baseline Phase 1 + ajouts edifio) : secrets `.env*`,
    `node_modules/`, `.next/`, `.vercel/`, `.supabase/`, `.turbo/`, tests, logs, TS
    incremental, `.claude/settings.local.json`.

11. **tsconfig durci** : `strict` + `noUncheckedIndexedAccess` + `noImplicitOverride` +
    `noFallthroughCasesInSwitch` + `forceConsistentCasingInFileNames` + target `ES2022`.
    Path alias `@/*` → `./src/*` conservé.

12. **Page d'accueil placeholder** : `src/app/page.tsx` minimale (titre `edifio Sourcing`,
    sous-titre étape 1/7, footer `© AlyoS Ingénierie 2026 — Outil interne`). Sera remplacée
    à l'étape 3 par `/login`. `<html lang="fr">`, robots `noindex,nofollow` jusqu'à mise
    en prod Gate 9.

13. **README.md** propre (stack, scripts, conventions, sécurité).

14. **DECISIONS.md mis à jour** : 5 entrées Gate 6 (plan validé, 2 projets Supabase
    distincts, Corepack shim, bootstrap stack, allowlist autonomie). Arbitrage 3 (projet
    Supabase) marqué résolu.

15. **Allowlist autonomie** dans `.claude/settings.local.json` : permissions larges pour
    les opérations Gate 6, deny strict sur `git push origin main`, `vercel --prod`,
    `supabase projects delete`, `gh repo delete`, `git reset --hard`, `rm -rf /*`.

## Points à vérifier avant commit

- `pnpm build` vert ✓ (vérifié post-scaffold)
- `pnpm lint` vert (à confirmer après les éditions de layout/page)
- `pnpm format:check` vert (à confirmer)
- `pnpm typecheck` vert (à confirmer)

→ Tâche #9 du plan : vérification locale finale avant commit.

## Demandes au Board

1. **Validation du commit `feat: bootstrap Next.js 14 + TS strict + Tailwind + outillage`
   sur `feat/init-nextjs`** avant push.
2. **Validation du push initial de `main` vers `origin/main`** (contient le seed Phase 1
   baseline) — autorisation exceptionnelle ponctuelle, deny `git push origin main`
   réactivé ensuite par PR-only.
3. Pas de PR ouverte dans cette étape — la PR `feat/init-nextjs → main` sera ouverte
   après étape 4 (quand Vercel sera branché et que la preview pourra valider).
   ⚠️  **Confirme-moi cette stratégie** : ouvrir la PR maintenant pour tracer ou
   attendre l'étape 4 ?

## Prochaine étape

**Étape 2 — `feat/middleware-domain`** : middleware Next.js à la racine, fonction pure
`isAuthorizedEmail()` testée Vitest, matcher couvrant tout sauf `/login` + `_next` +
assets + `/api/auth/*`, deny-by-default (pas de session → redirect `/login`). Branche
suivante créée depuis `feat/init-nextjs` mergé sur `main`.
