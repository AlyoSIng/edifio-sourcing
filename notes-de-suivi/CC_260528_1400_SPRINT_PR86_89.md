# Note de suivi — 2026-05-28 14h00 — Sprint PRs #86 + #87 + #88 + #89

**Agent** : Alex (`dev`) + Yann (`ps_operator`) — session Claude Code  
**Période** : 2026-05-28 matin → après-midi

---

## Actions réalisées

### PR #86 — Fix FORCE RLS `architects` (fix/architects-force-rls)

**Mergée le 2026-05-28.** Contenu :
- **Root cause** : `architects` a `relforcerowsecurity = true` depuis migration 0002.
  `fetchArchitectsPage` utilisait `db` brut sans `withTenantContext()` →
  `app.current_organization_id` jamais positionné → Postgres FORCE RLS bloquait
  toutes les lignes → page affichait "Annuaire indisponible".
- **Fix** : wrapping des requêtes SELECT + COUNT dans
  `withTenantContext(ALYOS_ORG_ID, dbClient, ...)` dans
  `src/app/sourcing/architectes/actions.ts`.
- **Signalement** : 4 autres fonctions du même fichier accèdent à `architects`
  avec `db` brut sans `withTenantContext` — à corriger en Phase 2.

### PR #87 — pgTAP `shortlist_criteria` + `tender_briefs` (test/pgtap-shortlist-briefs)

**Mergée le 2026-05-28.** Contenu :
- Nouveau fichier `tests/rls/11_shortlist_criteria_tender_briefs.sql` — 13 assertions :
  - `shortlist_criteria` (6) : FORCE RLS, SELECT sans JWT, isolation OrgA,
    test négatif cross-org OrgB, INSERT cross-tenant → `42501`
  - `tender_briefs` (7) : FORCE RLS, SELECT sans JWT, isolation OrgA,
    test négatif cross-org, filtre `is_active = true`, INSERT cross-tenant → `42501`
- **Bug corrigé en cours de CI** : `set_config('request.jwt.claims', '', true)` →
  `''` n'est pas du JSON valide → `current_organization_id()` crashait au cast
  `::jsonb`. Fix : `'{}'` (objet JSON vide → NULL pour `app_metadata.organization_id`).
- Demande CTO Sophie (Board 2026-05-28, PR #84 shortlist_criteria).

### PR #88 — Script PDF onboarding (feat/onboarding-pdf-assets)

**Mergée le 2026-05-28.** Contenu :
- Nouveau script `scripts/generate-pdf-assets.ts` (Tâche 2.3) :
  - Render Playwright Chromium headless de :
    - `design/copy/plaquette_commerciale_edifio_sourcing.html` → PDF A4
    - `design/copy/roadmap_produit_edifio_sourcing.html` → PDF A4
  - Upload `app-assets/onboarding/` (Supabase Storage, `upsert: true`)
  - Affiche les URLs publiques sur stdout
- Usage : `pnpm tsx scripts/generate-pdf-assets.ts`
- Requires `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` dans la
  session shell de Steve.
- **Étape suivante** : Steve lance le script → colle les URLs →
  Alex met à jour `src/db/seed/content-fixture.ts` → seed prod.

### PR #89 — Cookie `COOKIE_DOMAIN` Gate 7 (feat/gate7-cookie-domain)

**Mergée le 2026-05-28.** Contenu :
- `src/lib/supabase/server.ts` : `setAll` callback propagate `domain: process.env.COOKIE_DOMAIN`
- `src/middleware.ts` : idem sur `supabaseResponse.cookies.set`
- `.env.example` : `COOKIE_DOMAIN=` documenté (absent = comportement inchangé)
- **Action Yann post-merge** : ajouter `COOKIE_DOMAIN=.alyosingenierie.fr` dans
  les env vars Vercel **Production uniquement** (pas Preview, pas Development).
- Anticipation SSO Phase 2 : cookie partagé entre `sourcing.`, `suivi.`, `admin.alyosingenierie.fr`.

---

## État du main post-sprint

```
5a5c1ed feat(auth): cookie COOKIE_DOMAIN (#89)
7e3fa94 feat(onboarding): script PDF assets (#88)
7fb7c9a test(rls): pgTAP shortlist_criteria + tender_briefs (#87)
cf6f5dc fix(architects): FORCE RLS fetchArchitectsPage (#86)
be78269 feat(brief): brief IA + fix activeBrief + vocab (#85)
```

---

## Backlog mis à jour

| Item | Statut |
|------|--------|
| Tâche 1 — AO du jour par département | ✅ Livré |
| Tâche 2.1 — Brief IA | ✅ Livré (PR #85) |
| Tâche 2.2 — Rationale Haiku Tandem | ✅ Livré |
| Tâche 2.3 — PDFs plaquette + roadmap | 🟡 Script prêt (PR #88) — Steve doit lancer |
| Tâche 2.3 — Démo vidéo URL | ⏸ Manuel Board |
| pgTAP shortlist_criteria + tender_briefs | ✅ Livré (PR #87) |
| Fix Annuaire indisponible (FORCE RLS) | ✅ Livré (PR #86) |
| Gate 7 — cookie domain config | ✅ Code mergé (PR #89) — Yann pose env var Vercel |
| Gate 7 — DNS + Vercel domain custom | ⏸ Bloqué — Steve crée token Vercel |
| Scrapers PLACE + Francmarchés | 🟡 Phase 2 |
| Export PDF shortlist | 🟡 Phase 2 |
| Console superadmin unifiée | 🔴 Après Gate 6 |

---

## Actions en attente Board / Steve

1. **Gate 7 DNS** : créer token Vercel sur https://vercel.com/account/tokens
   → scope `alyos` → lancer les commandes CLI (cf. note précédente) →
   Yann configure CNAME + env var `COOKIE_DOMAIN=.alyosingenierie.fr`.
2. **Tâche 2.3 PDFs** : 
   ```powershell
   $env:NEXT_PUBLIC_SUPABASE_URL = "..."
   $env:SUPABASE_SERVICE_ROLE_KEY = "..."
   pnpm tsx scripts/generate-pdf-assets.ts
   ```
   Coller les URLs → Alex met à jour `content-fixture.ts`.
3. **Backfill postal_code** (basse priorité) :
   `$env:DATABASE_URL = "..." ; pnpm tsx scripts/backfill-postal-code.ts`
