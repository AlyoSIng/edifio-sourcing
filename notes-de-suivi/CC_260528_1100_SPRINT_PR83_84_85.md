# Note de suivi — 2026-05-28 11h00 — Sprint PRs #83 + #84 + #85

**Agent** : Alex (`dev`) + Yann (`ps_operator`) — session Claude Code  
**Période** : 2026-05-27 nuit → 2026-05-28 matin

---

## Actions réalisées

### Correction migrator + journal

- **Bug détecté** : le migrator custom `runMigrationsPerTransaction` comparait `folderMillis` au `MAX(created_at)` de `__drizzle_migrations`. Les migrations 0010-0024 ayant `when = 1748xxx` (< 0009's `when = 1779xxx`), elles étaient **silencieusement skippées**.
- **Fix** : timestamps 0025/0026/0027 corrigés dans `_journal.json` → `1779731000000 / 1779731001000 / 1779731002000`.
- **Vérification prod** (Supabase MCP) : 28 hashes dans `drizzle.__drizzle_migrations` = 28 entrées journal → **toutes les migrations 0000-0027 sont bien appliquées**.

### PR #83 — Pipeline C/R + brief AO v2 (feat/cr-brief-v2-rc)

**Mergée le 2026-05-28.** Contenu :
- Module Conception-Réalisation : `CrKanbanBoard`, `page-data.ts`, route `/sourcing/conception-realisation`
- `RcAnalysisCard.tsx` : affichage analyse RC structurée (pièces, échéances, critères, compétences, alertes)
- Prompt `rc_analysis_full` v2 : champ `competences_demandees` ajouté
- pgTAP `06_ai_prompts_seed` : tests adaptés (invariant sémantique, `MAX(version) = 2`)
- Fix lockfile (`@esbuild/win32-arm64` optionalDependencies)

### PR #84 — Shortlist criteria (feat/shortlist-criteria)

**Mergée le 2026-05-28.** Contenu (Nadia) :
- Table `shortlist_criteria` (migration 0027) : critères paramétrables par organisation et cible
- Page admin `/sourcing/admin/shortlist`
- Résolution conflit `_journal.json` post-merge PR #83

### PR #85 — Brief IA détail AO + fix activeBrief null + vocab (feat/brief-detail-vocab)

**Ouverte le 2026-05-28.** Contenu :
- **Fix critique** : `getTendersOfTheDay` retournait toujours `activeBrief: null`. Fix : LEFT JOIN `tender_briefs WHERE is_active = true` (pas de N+1)
- **Feature** : section "Brief IA" + `BriefGenerator` sur la page `/sourcing/ao/[id]`
- **Vocab** : derniers "Tandem"/"Solo" → "Cotraitance"/"Mandataire" dans 4 fichiers UI
- Tests `queries.test.ts` étendus (LEFT JOIN mock + propagation `activeBrief`)

---

## État du backlog post-sprint

| Item | Statut |
|------|--------|
| Tâche 1 — AO du jour par département | ✅ Livré (main) |
| Tâche 2.1 — Brief IA | ✅ Livré (PR #85 en cours CI) |
| Tâche 2.2 — Rationale Haiku Tandem | ✅ Livré (tandem/actions.ts `createHaikuRationaleClient`) |
| Tâche 2.3 — Contenu onboarding fixture | ⏸ Bloqué — URLs Board attendues |
| Arbitrage CTO — FORCE RLS + withTenantContext | ✅ Livré (PR #52 mergée, migration 0018 appliquée) |
| Arbitrage CTO — audit_action +3 valeurs | ✅ Livré (migration 0021) |
| RLS hardening cotraitants/be_documents | ✅ Livré (migration 0018 appliquée en prod) |
| Moulinette Pappers | ✅ Implémentée (bouton admin, PR #74) — à relancer si nouvelles fiches |
| Scrapers PLACE + Francmarchés | 🟡 Phase 2 |
| Export PDF shortlist | 🟡 Phase 2 |
| Console superadmin unifiée | 🔴 Après Gate 6 |

---

## Actions en attente Board

1. **Merging PR #85** (feat/brief-detail-vocab) une fois CI verte — 🟢 peut merger
2. **Contenu onboarding** (`content-fixture.ts`) : renseigner les URLs (vidéo démo, plaquette, roadmap) → déclencher script prod
3. **Relancer moulinette Pappers** si de nouveaux architectes ont été ajoutés (bouton admin `/sourcing/architectes`)
4. **Backfill `postal_code`** (Steve) : `pnpm db:backfill:postal-code` — `department` déjà backfillé (107/107), `postal_code` sera null pour la plupart (Opendatasoft ne fournit pas les CP)

---

## Vocabulaire acté (Board 2026-05-27)

- **Solo** → **Mandataire** (réponse en propre)
- **Tandem** → **Cotraitance** (mobilisation architecte partenaire)
- **C/R** → **Conception-Réalisation** (groupement MOE + réalisateur)
- Identifiants internes DB/code inchangés (coût de migration élevé, Phase 2)
