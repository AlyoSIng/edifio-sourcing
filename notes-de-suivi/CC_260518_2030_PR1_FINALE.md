# Étape 6/6 module sourcing engine — Fix CI PGDG + clôture 1re PR

**Date** : 2026-05-18 20:30
**Auteur** : Alex (DEV) — orchestrateur Claude Code (sans délégation sub-agents, cf. consigne Board 2026-05-18 pour éviter le crash socket de la tentative précédente)
**Branche** : `feat/sourcing-mvp`
**Référence amont** : `DECISIONS.md` 2026-05-18 (verdict CTO ADR-013) + `specs/adr_013_orm_drizzle.md` + `specs/module_sourcing_engine_v1.md`
**Étape précédente** : 5/6 — Seed dev/CI 2 orgs + 200 tenders + fixture BOAMP réelle (commit `5e4fd9f`)
**Statut** : 1re PR module sourcing engine **prête à ouvrir** vers `main`

---

## Synthèse exécutive

Étape 6/6 livrée et committée localement (commit `e20eae9`) : fix CI ajoutant le dépôt officiel PGDG (`apt.postgresql.org`) à l'étape « Install pgTAP + pg_prove » du workflow `db-rls.yml`. Le runner GitHub `ubuntu-latest` est passé sur Noble 24.04, qui n'expose plus `postgresql-15-pgtap` dans le dépôt universe — la CI plantait avant même d'atteindre les étapes `migrate → seed → test:rls`.

Avec ce fix, la pipeline `ci-db-rls` peut désormais matérialiser sur Postgres réel la **condition 2 du verdict CTO ADR-013** (médiane `raw_data` ∈ [20-30] KB sur dataset seedé 2 orgs × 100 tenders). Les 6 étapes de la 1re PR module sourcing engine sont closes.

---

## Récap des 6 étapes — 1re PR module sourcing engine

| # | Commit | Sujet | Livré |
|---|---|---|---|
| 1 | `511c87e` | Bootstrap Drizzle tooling | `drizzle-orm@0.39`, `drizzle-kit@0.30`, `postgres@3.4` (pinned), `drizzle.config.ts`, `src/db/client.ts` |
| 2 | `0a1afcc` | Migration `0000_init` | Enum `subscription_tier('sourcing','cotraitance','studio')` |
| 3 | `40bbfc9` | Schema Drizzle v1 complet | 11 fichiers `src/db/schema/{ai,architects,audit,config,enums,integrations,library,organizations,selections,tenders,users}.ts` + barrel `index.ts` + types `src/db/types/jsonb.ts` (9 colonnes `jsonb().$type<T>()`) + migration `0001_schema_v1.sql` (22+ tables) + tests inference |
| 4 | `760e2d2` | RLS FORCE + 20 policies + FK `auth.users` | `0002_rls.sql` (helpers `current_organization_id()` / `current_user_role()`, FORCE sur 19 tables, 20 policies tenant_isolation + 1 insert_by_member, triggers immutabilité audit_logs + 5 triggers `touch_updated_at`) + `0003_fk_supabase.sql` (FK cross-schema `users.id → auth.users.id ON DELETE CASCADE`) + 5 tests pgTAP `tests/rls/00..04` |
| 5 | `5e4fd9f` | Seed dev/CI 2 orgs + 200 tenders + fixture BOAMP réelle | `src/db/seed/{index,reset,fetch-boamp-fixture}.ts` + `lib/{anonymize,distribution,fixture-loader,fixture-mock}.ts` + `fixtures/boamp-real.json` (~10,4 MB anonymisé) + `tests/rls/02_tenant_isolation.sql` étendu (9 assertions) + `tests/rls/04_audit_immutable.sql` + workflow `db-rls.yml` étendu `migrate → seed → test:rls` |
| 6 | `e20eae9` | Fix CI PGDG (cette étape) | `db-rls.yml` : ajout dépôt officiel `apt.postgresql.org` à l'étape « Install pgTAP » pour rétablir `postgresql-15-pgtap` sur Ubuntu Noble |

---

## Mapping demande Board (a)(b)(c)(d) → livré

| Demande | Livré | Référence |
|---|---|---|
| **(a)** Migration `0000_init.sql` avec `CREATE TYPE subscription_tier AS ENUM`, `ALTER TABLE organizations ADD COLUMN subscription_tier NOT NULL DEFAULT 'studio'`, `CREATE INDEX idx_organizations_tier` | ✅ Posé en deux migrations cohérentes : `0000_init.sql` (enum) + `0001_schema_v1.sql` (colonne + index) — le split est imposé par `drizzle-kit generate` qui isole les enums dans une migration dédiée | `src/db/migrations/0000_init.sql`, `0001_schema_v1.sql` |
| **(b)** Schema Drizzle v1 dans `src/db/schema.ts` — toutes les tables de `specs/schema_v1.sql` avec typage jsonb fort via `$type<T>()` pour les 9 colonnes jsonb | ✅ Posé en arborescence éclatée par domaine (`src/db/schema/*.ts` + barrel `index.ts`) — décision Board étape 3 pour faciliter la navigation 22+ tables. Typage jsonb fort sur les 9 colonnes via `src/db/types/jsonb.ts` | `src/db/schema/*.ts`, `src/db/types/jsonb.ts` |
| **(c)** RLS FORCE 12 policies dans `src/db/rls/policies.sql` (SQL natif hors ORM) — helpers `current_organization_id()` et `current_user_role_text()` | ✅ Posé en `src/db/migrations/0002_rls.sql` (migration custom Drizzle, SQL natif) — **écart documenté** : 20 policies tenant_isolation au lieu de 12 (toutes les tables multi-tenant de `specs/schema_v1.sql` couvertes, pas seulement le périmètre minimal du spike). Helpers `current_organization_id()` (uuid) et `current_user_role()` (cast typé `membership_role`, plus solide que la version `_text()` du spike). | `src/db/migrations/0002_rls.sql` |
| **(d)** Seed avec payload Opendatasoft réel à 25 KB médiane (distribution 15 % 10 KB / 60 % 25 KB / 25 % 45 KB) | ✅ Buckets calibrés `small 9-11 KB / medium 22-28 KB / large 42-48 KB`, ratios 15/60/25, médiane bornée `[20-30] KB` avec `assertDistribution()` bloquant. Fixture BOAMP réelle anonymisée (10,4 MB committé pour reproductibilité CI sans dépendance réseau). | `src/db/seed/lib/distribution.ts`, `src/db/seed/fixtures/boamp-real.json` |

---

## Statut des 3 conditions formelles du verdict CTO (ADR-013)

| Condition | Statut | Action |
|---|---|---|
| **1. Bench cold start Edge Function Supabase Deno réel** (preview Vercel + k6 + sonde dédiée — seuil ≥ 200 ms d'écart Drizzle vs Prisma) | ⏸ Bloquant **pré-Gate 9**, hors scope de cette PR | Planifié sur la phase pré-Gate 9 (workflow dédié à créer). Suivi DECISIONS.md batch n°11 ligne 1. |
| **2. Re-seed payload Opendatasoft réel à 25 KB médiane** | ✅ Matérialisée par cette PR | Code en place (commit `5e4fd9f`), `assertDistribution()` bloquant en seed. **Exécution Postgres réelle** = sortie du workflow `ci-db-rls` après push de cette branche (étape suivante). |
| **3. Conservation des branches `spike/orm-drizzle` et `spike/orm-prisma` jusqu'au 2026-06-17** | ✅ Conservées sur `origin` (visibles dans `git branch -a`) | Cleanup différé programmé pour le 2026-06-17 (à la charge de Yann, cf. DECISIONS.md batch n°11). |

---

## Hors scope (intentionnel) de cette 1re PR

Ces items appartiennent aux PR suivantes du module sourcing engine (cf. tableau §6 de `specs/module_sourcing_engine_v1.md`) :

- Connecteur BOAMP `src/lib/sourcing/connectors/boamp.ts` (API Opendatasoft)
- Connecteurs scraping PLACE / Francmarchés / MP.info (délégation au container Fly.io)
- Pipeline `normalize → dedup → filter → score → insert` (`src/lib/sourcing/*.ts`)
- Cron Vercel `/api/cron/sourcing-run` (POST `30 6 * * 1-5` Europe/Paris)
- Webhook scraper Fly.io `/api/webhooks/scraper-done`
- Tests E2E scénario S1 du plan de recette Gate 7

---

## Validation locale (Phase 1 — re-vérifiée après commit étape 6)

- `git status` : working tree propre après commit `e20eae9`.
- `git log feat/sourcing-mvp ^main --oneline` : 6 commits proprement séparés étape par étape.
- `eslint` : 0 warning ni erreur (sortie automatique du commit hook).
- Pas de `tsc --noEmit` ni `vitest run` ni `next build` ré-exécuté à cette étape — la modification 6/6 n'a touché aucun fichier TS / TSX / JS (uniquement YAML + MD). La validation Phase 1 verte de l'étape 5/6 (commit `5e4fd9f`) reste donc valide.

---

## Prochaines actions immédiates

1. **Push `feat/sourcing-mvp` → `origin`** pour déclencher la pipeline `ci-db-rls` étendue → matérialise la condition 2 sur Postgres réel.
2. **Ouverture PR** `feat/sourcing-mvp → main` titrée « feat(db): module sourcing engine — schema Drizzle v1 + RLS FORCE + seed BOAMP réel ». Corps de PR avec :
   - Le tableau de mapping (a)/(b)/(c)/(d) ci-dessus
   - La checklist des 3 conditions CTO
   - Référence ADR-013 + lien `DECISIONS.md` batch n°11
3. **Revue CTO Sophie** + Board OK → merge sur `main`.
4. Démarrage **PR n°2** du module sourcing engine : connecteur BOAMP + normalisation + insertion (étape 1 du tableau §6 de `specs/module_sourcing_engine_v1.md`).

---

*Note rédigée en orchestrateur Claude Code direct, sans délégation sub-agents (consigne Board 2026-05-18 — éviter le crash socket de la tentative précédente). Toutes les actions de cette étape sont locales (commit) ou seront soumises à l'OK Board avant exécution distante (push + PR).*
